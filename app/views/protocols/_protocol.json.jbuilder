json.extract! protocol, :id, :main_number, :second_number, :customer_id, :sample, :date_receipt, :date_transfer_lab, :date_result, :date_pay, :urgent, :paid, :send_to_customer, :comment, :created_at, :updated_at
json.url protocol_url(protocol, format: :json)